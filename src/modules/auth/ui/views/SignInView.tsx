import { LoginButton } from "@/components/shared/navbar/components/LoginButton";

const SignInView = () => {
  return (
    <div className="h-screen flex items-center justify-center">
      <LoginButton />
    </div>
  );
};

export default SignInView;
